


export class ProgressBar {

  constructor() {

  }

  private setValue() {
    //TODO
    //Actually edits the html element (this singleton class owns it)
  }

  public setProgress(completedTasks: string[], pendingTasks: string[], secondsOnCurrentTask: number = 0.0) {
    
  }

  public sampleTaskCompletionTime(taskName: string, seconds: number) {
    //Yeah this task keeps track of how long usually tasks with same name take, and do the computations itself
  }

}